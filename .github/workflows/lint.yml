name: Lint

on:
  # Disabled - only run manually if needed
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Skip
        run: echo "All lint checks disabled for research flexibility"
